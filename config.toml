# The URL the site will be built for
base_url = "https://mboleary.dev"

# Because Github has stupid artificial limitations on what subdirectories can be published
output_dir="docs"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "arsmateria-zola-theme"

author = "Brady O'Leary"

[markdown]
# highlight_code = true
# Whether to do syntax highlighting
# Theme can be customized by setting the `highlight_theme` variable to a theme supported by Zola
title = "Brady O'Leary"


[extra]
# Put all your custom variables here

copyright_year = 2024
default_description = "This is Brady's landing page"
# Menu Config
menu_data_path="menu.json" # Load menu data from a path
# Tag config
tag_data_path="tag.json" # Load menu data from a path
keywords=["developer"]